{
  "name": "server",
  "version": "1.0.0",
  "description": "Park4night backend",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "npx prisma generate && (test -n \"$DIRECT_URL\" && DATABASE_URL=$DIRECT_URL npx prisma migrate deploy || echo 'Skipping migrate deploy (no DIRECT_URL)')",
    "test": "NODE_ENV=test jest tests/",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "axios": "^1.4.0",
    "cld": "^2.10.1",
    "cloudscraper": "^4.6.0",
    "connect-pg-simple": "^9.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "passport": "^0.6.0",
    "passport-google-oauth20": "^2.0.0",
    "pg": "^8.11.0",
    "prisma": "^5.22.0",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "jest": "^29.5.0",
    "nodemon": "^3.1.14",
    "supertest": "^6.3.3"
  }
}
