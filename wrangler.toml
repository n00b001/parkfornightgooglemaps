name = "parking"
main = "worker/index.ts"
compatibility_date = "2024-12-01"

# Serve the Vite build as static assets with SPA routing
[assets]
directory = "client/dist"
not_found_handling = "single-page-application"
