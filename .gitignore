# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Production
dist/
build/
client/dist/
server/dist/

# Misc
*.log
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Optional npm cache
.npm

# Optional REPL history
.node_repl_history

# OMC state
.omc/

# Logs
logs/

# PI Lens
.pi-lens/

# Scraped data files (too large for git, uploaded to Firestore instead)
scripts/data/*.jsonl
scripts/data/*.log
scripts/data/checkpoint.json
scripts/data/places_export.json
scripts/data/reviews_export.json
# Place images (hundreds of MBs/GBs - uploaded to Firestore)
scripts/data/images/places/
# Keep vehicle icons (small, needed for avatars)
!scripts/data/images/icons/
!scripts/data/images/.gitkeep

# Downloaded images - tracked in git for deployment
# Vehicle icons (small, needed for avatars)
!scripts/data/images/icons/
.playwright-cli/
__pycache__/
firebase-credentials.json
scripts/r2-config.json

# Normalised data (generated by normalize.py, too large for git)
scripts/data/normalized/

# Upload script config (contains credentials)
scripts/upload/r2-config.json

# Python virtual environments
.venv/
scripts/.venv/
scripts/normalize/.venv/
scripts/upload/.venv/

# uv lock files (regenerated on each machine)
scripts/normalize/uv.lock
scripts/upload/uv.lock
