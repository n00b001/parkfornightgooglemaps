{
  "name": "server",
  "version": "1.0.0",
  "description": "Prisma schema and migrations for Park4Night",
  "private": true,
  "scripts": {
    "migrate": "npx prisma migrate deploy",
    "generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  }
}
